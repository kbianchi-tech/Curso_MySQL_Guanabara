# 📚 Anotações do Curso de MySQL - Curso em Vídeo

Este repositório serve como um registro pessoal e material de consulta para os meus estudos do curso de **Banco de Dados MySQL**, ministrado por **Gustavo Guanabara** na plataforma **Curso em Vídeo**.

O conteúdo principal reflete o meu entendimento pessoal sobre os comandos e conceitos apresentados durante as aulas, servindo como um guia rápido para referência futura.

## 📖 Conteúdo Principal

O coração deste repositório está no arquivo `MySQL.md`, que contém um resumo abrangente dos comandos SQL, organizado da seguinte forma:

* **DDL (Data Definition Language):** Comandos para criar e modificar a estrutura do banco (`CREATE`, `ALTER`, `DROP`).
* **DML (Data Manipulation Language):** Comandos para gerenciar os dados (`INSERT`, `UPDATE`, `DELETE`).
* **DQL (Data Query Language):** Comandos para consultar dados, incluindo `SELECT`, `WHERE`, `ORDER BY` e operadores detalhados (`LIKE`, `BETWEEN`, `IN`, `AND`/`OR`).
* **JOINs:** Explicações sobre como combinar tabelas com `INNER JOIN`, `LEFT JOIN` e `RIGHT JOIN`.
* **Conceitos:** Definições de Chaves Primárias e Estrangeiras.

## 📂 Estrutura do Repositório

O projeto está organizado da seguinte maneira para facilitar a consulta:

* `/Aulas_e_Comandos/MySQL.md`: O arquivo central com todas as anotações detalhadas dos comandos.
* `/Imagem/`: Pasta contendo as imagens e tabelas de referência (como operadores lógicos, comparativos, etc.) que são usadas como apoio visual no arquivo de anotações.